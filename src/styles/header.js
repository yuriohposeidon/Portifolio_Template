import styled from "styled-components";

export const StyledHeader = styled.header`
  display: flex;
  align-items: center;

  padding-left: 0.5rem;

  background-color: var(--color-gray-1);

  box-shadow: 0rem 0.4rem 3.2rem -1.2rem rgba(0, 0, 0, 0.25);

  height: 8.8rem;

  margin-bottom: 4rem;

  .logo__container {
    width: 80%;
    margin: 0 auto;
  }
`;
